# 验证与交付报告

## 1. 变更摘要

- 任务/关联编号：`<TASK_ID>`
- 变更类型：`<TYPE>`
- 实际变更文件：
- 未包含的范围：
- 目标环境和版本：

## 2. 检查环境

| 项目 | 内容 |
| --- | --- |
| 操作系统/运行环境 | `<ENV>` |
| Java/Maven/Node/pnpm 版本 | `<VERSIONS>` |
| 配置来源 | `<AGENTS_OR_DEV_ENV_OR_PROJECT_CONFIG>` |
| 规范指纹 | `<FINGERPRINT_OR_NOT_APPLICABLE>` |
| 工作区状态 | `<STATUS_SUMMARY>` |

## 3. 检查结果

| 检查 ID | 来源 | 范围 | 实际命令 | 退出码/状态 | 证据 | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| `<CHECK_ID>` | `<SOURCE>` | `<CHANGED/FULL>` | `<ACTUAL_COMMAND>` | `<EXIT_CODE_OR_REASON>` | `<OUTPUT_OR_PATH>` | `通过/阻断/告警/未知/未执行` |

只填写实际执行的命令。工具缺失、超时、路径不匹配和环境问题必须写入状态与原因。

## 4. 契约和文档

- API 是否变化：`是 / 否 / 待确认`
- 唯一接口文档路径或无需更新原因：
- 数据库/消息/配置/序列化是否变化：
- 修订号、迁移和兼容性说明：
- 相关文档和模板：

## 5. 安全、发布和回滚

- 敏感数据/权限影响：
- 外部调用、重试、幂等和可观测性：
- 数据迁移和发布顺序：
- 代码回滚：
- 配置回滚：
- 数据恢复/补偿：
- 外部副作用处理：

## 6. 未完成项和残留风险

| 项目 | 原因 | 影响 | 后续动作 | 责任人 | 截止时间 |
| --- | --- | --- | --- | --- | --- |
| `<ITEM>` | `<REASON>` | `<IMPACT>` | `<NEXT_STEP>` | `<OWNER>` | `<DATE>` |

## 7. 最终结论

- 结论：`通过 / 带条件交付 / 阻断 / 待补充证据`
- 结论适用范围：
- 风险接受人：
- 交接 Artifact/报告路径：
- 交付说明：
