# 通用设计与实现方向

当任一 Skill 同时涉及设计与实现时，必须遵守以下方向：

- 设计必须自上而下：先明确目标、边界和公共契约，再逐层拆分为模块、组件、接口和可独立验收的任务。
- 实现必须自下而上：先完成并验证底层模型、基础设施、数据访问或可复用原子组件，再逐层组装业务服务、接口/页面和端到端流程。
- 每一层完成后必须进行与该层对应的最小充分验证；不得跨越未验证的底层依赖直接编排上层功能。
- 若受现有架构约束无法遵守，必须记录偏离原因、影响和替代验证证据。

只读审查、纯测试或其他不包含实现的任务不强行新增实现步骤，但若任务中出现实现环节，仍必须按上述方向执行。
