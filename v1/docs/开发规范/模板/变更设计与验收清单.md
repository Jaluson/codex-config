# 变更设计与验收清单

> 用于功能开发、缺陷修复、重构、测试补充、性能优化和升级迁移。填写真实内容；无法确认的项目标记为“待确认”，不要用推测替代证据。

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 任务/关联编号 | `<TASK_ID>` |
| 变更类型 | `功能 / 缺陷 / 重构 / 测试 / 性能 / 升级` |
| 负责人 | `<OWNER>` |
| 审查者 | `<REVIEWER>` |
| 目标项目/技术栈 | `<PROJECT>` / `springboot / vue` |
| 目标环境和版本 | `<ENV_AND_VERSIONS>` |
| 日期 | `<YYYY-MM-DD>` |

## 2. 目标和范围

- 目标：
- 成功标准：
- 任务范围：
- 明确不做：
- 受影响模块/调用方：
- 约束和假设：

## 3. 现状证据

- 当前行为/问题：
- 复现步骤或基线：
- 事实来源（代码、测试、配置、日志、指标）：
- 用户已有修改和工作区状态：
- 规范、契约和工具来源：

## 4. 契约和设计

- 输入和校验责任：
- 输出、错误和状态变化：
- 权限、租户和资源边界：
- 事务、异步、幂等和重试：
- API/数据库/消息/配置契约变化：`有 / 无 / 待确认`
- 唯一接口文档路径或“无需更新”原因：
- 数据迁移、兼容窗口和清理条件：
- 回滚、恢复或补偿方式：

## 5. 实现验收清单

- [ ] 只修改任务范围内的文件。
- [ ] 新增或修改文本使用 UTF-8。
- [ ] 输入、权限、异常和失败路径有明确责任边界。
- [ ] 未硬编码密钥、令牌、生产数据或环境专用值。
- [ ] API、数据、配置、消息和序列化契约已同步。
- [ ] 测试覆盖正常、失败、边界、权限、重复/并发中适用的场景。
- [ ] 文档、迁移、监控和发布说明已同步。
- [ ] 已记录未执行项、环境阻塞和残留风险。

## 6. 风险记录

| 风险 | 影响 | 可能性 | 缓解措施 | 责任人 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `<RISK>` | `<IMPACT>` | `低/中/高` | `<MITIGATION>` | `<OWNER>` | `开放/已缓解/接受` |

## 7. 验收条件

1. `<可观察的验收条件>`
2. `<失败或边界条件>`
3. `<兼容性/回滚条件>`
