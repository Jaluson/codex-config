# ${模块}-${功能} 接口文档

> 本文档只保留当前接口契约；历史变化记录在文末“修订记录”中。

## 接口概览

| 项目 | 内容 |
| --- | --- |
| 模块 | ${模块} |
| 功能 | ${功能} |
| 服务/前缀 | ${服务名或基础路径} |
| 当前修订 | R1 |
| 最近更新 | YYYY-MM-DD |

## 鉴权与通用约定

- 鉴权方式：${鉴权方式}
- 通用请求头：${请求头}
- Content-Type：${请求格式}
- 分页、排序和幂等约定：${通用约定}
- 通用错误格式：${错误结构}

## 接口详情

### ${HTTP 方法} ${接口路径}

#### 用途

${接口用途}

#### 权限与兼容性

- 所需权限：${权限}
- 幂等性：${幂等说明}
- 兼容性/弃用说明：${兼容性说明}

#### 请求

| 参数位置 | 参数名 | 类型 | 必填 | 说明 | 示例 |
| --- | --- | --- | --- | --- | --- |
| path/query/header/body | ${参数名} | ${类型} | 是/否 | ${说明} | ${示例} |

请求示例：

```http
${请求示例}
```

#### 成功响应

HTTP 状态码：`${状态码}`

```json
{
  "${字段}": "${示例值}"
}
```

| 字段 | 类型 | 必有 | 说明 |
| --- | --- | --- | --- |
| ${字段} | ${类型} | 是/否 | ${说明} |

#### 失败响应

| HTTP 状态码 | 错误码 | 触发条件 | 响应说明 |
| --- | --- | --- | --- |
| ${状态码} | ${错误码} | ${条件} | ${说明} |

## 修订记录

| 修订号 | 日期 | 修改内容 | 影响接口 | 原因/关联任务 |
| --- | --- | --- | --- | --- |
| R1 | YYYY-MM-DD | 初次建立接口文档 | ${接口路径} | ${原因或任务} |
