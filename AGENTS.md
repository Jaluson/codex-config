# Agent 工作规则

## 子代理协作门禁

- 主代理拥有唯一的需求、方案、优先级、范围、验收标准和流程决策权，只负责任务拆分、委派、结果审阅和最终交付。
- 主代理不得直接执行仓库读取、文件修改、命令、测试或验证：读取、检索和定位必须委派给 `explorer`；修改、命令、测试和验证必须委派给 `worker`。
- 每次委派必须是边界明确的原子任务，至少说明唯一目标、允许范围、输入、预期输出、验收条件和依赖关系。
- 主代理应并行委派互不依赖且不会写入同一目标的原子任务；存在依赖、写入冲突或决策点的任务必须等待主代理作出决定后再继续。
- 子代理只能回传事实、证据和执行结果，不得自行改变目标、方案、范围、优先级、验收条件或后续流程，也不得派发子代理。
- 仅无法由项目规则覆盖的上级系统、安全或运行时权限要求可以构成主代理直接操作例外；发生例外时必须在最终交付中说明原因和实际操作。

## 编码规范

- 所有新增或修改的文本文件必须使用 UTF-8 编码。
- 读取或写入文件时，显式指定 UTF-8，避免使用操作系统默认编码。
- 不对与当前任务无关的既有文件进行批量编码转换。

## 语言规范

- Agent 与用户的交流使用中文。
- 新增或修改的项目文档和代码注释使用中文。
- 代码标识符、第三方名称、协议字段和工具命令保留项目生态中的惯用写法。

## 开发规范与工具门禁

- 项目根目录 `docs/开发规范/README.md` 是轻量工程原则和规则来源治理的入口，不穷举空格、注释、命名或日志格式。
- 格式、命名和静态质量优先使用项目已有的格式化器、Lint、类型检查、测试、构建和架构检查工具；没有可靠来源时不得凭个人偏好新增规则。
- 使用 `development-standards` 或 `$orchestrator` 时，先发现项目现有工具并计算当前规范来源 Hash，再执行变更范围检查和适用的全量检查；默认不得运行会改写源文件的格式化命令，只有用户明确授权时才可使用受限 `--fix`。
- 工具失败、检查未执行或环境阻塞必须如实记录，不能报告为通过；既有代码样例只能作为低置信度参考。

## 默认开发环境

- 执行构建、测试、运行脚本或其他项目命令前，必须先读取项目根目录的 `.dev-env.yaml`。
- 将 `.dev-env.yaml` 中的 `java.home` 和 `maven.home` 分别作为 `JAVA_HOME` 和 `MAVEN_HOME` 的默认值，并在当前命令进程中使用对应工具路径。
- 当前配置的默认环境为 JDK 25 和 Maven 3.9.15；执行命令前应检查配置路径存在，并以实际版本输出为准。
- 用户在任务中明确指定的版本、路径或命令参数优先于 `.dev-env.yaml`。
- `.dev-env.yaml` 缺失或无法解析时，必须先说明原因；在不猜测配置值的前提下，可使用已验证的环境默认值继续执行。
- 不修改系统级环境变量；环境配置仅对当前项目命令或当前进程生效。
